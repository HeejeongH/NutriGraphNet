# Handoff — 2026-08-10 (다른 컴퓨터에서 이어가기용)

이 파일은 Claude Code가 Mac에서 작업하던 걸 GPU 컴퓨터에서 이어가기 위한 인계 노트입니다.
새 세션에서 "이 파일 읽고 이어서 진행해줘"라고 하면 됩니다.

## 논문 현재 상태
- v2.2 (커밋 fae3719, 2026-07-18) 확정: HFRS-DA가 AUC/NDCG/MRR에서 전 밀도 NutriGraphNet을
  앞선다는 실측 반영, "정직한 두-모델" 프레임 (§Abstract, §11.6, Finding H2).
- SOTA 튜닝 5라운드(cosine decoder/health-loss/shallower encoder/InfoNCE/content-anchor)
  전부 HFRS-DA NDCG@10=0.625 미달로 종료됨 (§8.4 Finding H2).

## 이번 세션에서 한 일
1. **버그 발견+수정 (미커밋)**: `src/nutrigraphnet_v2.py`의 `evaluate_model()`이
   `model.encoder()`/`model.decoder()`를 직접 호출해서, `NutriGraphNetV2.forward()`에만
   있던 content-anchor 잔차융합을 평가 시점엔 건너뛰고 있었음 (학습 땐 적용, 평가 땐 스킵).
   → v4 content-anchor 실험 결과(Finding H2의 NDCG 0.408/0.400)가 잘못 측정됐을 가능성.
   `encode()`/`score()` 메서드를 신설해 `forward()`와 `evaluate_model()`이 공유하도록 고침.
   `python3 -m py_compile` 통과. **아직 재평가는 안 함.**
2. **References 완료 (2026-07-30자, 미커밋)**: `논문/draft/paper_draft.md`의 References
   16개를 실제 논문과 대조해 채움 + 오귀속 정정(FRMADHG → Alkhrijah et al.).

## 다음 할 일 (우선순위 순)
1. **재평가 우선** (GPU 불필요, CPU 추론으로 충분): 로컬에 남은 체크포인트
   `results/gpu/V4_ca_cos_100pct/full_ca_cos/fold_*/best_model.pth`를 고쳐진
   `evaluate_model`로 다시 돌려서 content-anchor의 진짜 NDCG를 확인. 재학습 불필요 —
   같은 fold 분할(seed=42)로 test_data만 재구성해서 `evaluate_model(model, test_data,
   criterion, device, compute_rank=True)` 호출하면 됨.
2. **Retrieve-then-Rerank 2단계 파이프라인 구현** (사용자가 확정한 방향, 아직 미착수):
   - 문제: food 임베딩이 64차원 중 ~11개 유효차원에만 압축됨(협업 메시지패싱) →
     retrieval(HR)엔 유리, ordering(NDCG/MRR)엔 불리 (Finding H2 진단).
   - 설계: stage 1 = 기존 디코더 (101개 후보, leave-one-out sampled ranking —
     `ranking_metrics_from_z()` 참고). stage 2 = raw 영양소 features + ingredient
     attention pool로 만든 **content-only** food 벡터(collaborative encoder 안 거침,
     `_content_food()`와 유사하되 z_dict에 fuse 안 하고 독립 점수로 top-N만 재정렬) —
     content-anchor가 인코더 이전에 fuse해서 실패했던 것과 달리, 디코더 이후 late-fusion
     으로 설계해야 같은 실패 메커니즘을 반복 안 함.
   - 훈련: base 모델 frozen, reranker는 shortlist 기반 listwise/softmax loss로 별도 학습
     (기존 5차 시도는 전부 BPR pairwise loss로 실패 — BPR은 NDCG 같은 fine ordering에
     약하다는 정황과 일치. 다른 loss 메커니즘이 핵심 차별점).
3. 결과 나오면 §8.4 Finding H2 갱신, 위 두 미커밋 변경사항과 함께 커밋 여부 확인.

## 저장소
- GitHub: `https://github.com/HeejeongH/NutriGraphNet.git`
- 로컬 브랜치: `genspark_ai_developer`
